- [x] Confirm classification inference path and model loading logic.
- [x] Fix `torch.load` handling in `backend/app.py` (removed `weights_only=True`).
- [x] Repair `backend/app.py` indentation/syntax by rewriting file.
- [ ] Restart backend and validate `/predict` loads weights successfully.
- [ ] Verify frontend sends correct `data:image/...;base64,...` and that the endpoint is used consistently.

